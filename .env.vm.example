# VM deployment environment variables
# Copy to .env on the VM and fill values.

# Required
SECRET_KEY=CHANGE_ME

# Set this to: <VM_EXTERNAL_IP>,localhost,127.0.0.1 (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1

# Optional admin auto-provision (non-interactive)
# PROVISION_ADMIN_ON_STARTUP=true
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=ChangeMeNow
# ADMIN_EMAIL=admin@example.com
